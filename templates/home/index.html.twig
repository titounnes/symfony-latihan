{% extends 'base.html.twig' %}

{% block title %}Hello HomeController!{% endblock %}

{% block body %}

<div class="jumbotron">
    <h1>Wellcome to Lesson Plan Project on <a href="https://e-project-tech.com">eProject</a>! ✅</h1>

    {% if app.user %}
        <div class="mb-3">
            You are logged in as {{ app.user.username }} 
        </div>
    {% else %}
        <div class="mb-3">
            Already have an account? <a href="{{ path('app_login') }}">Login</a>
        </div>
        <div class="mb-3">
            Need an account? <a href="{{ path('app_register') }}">Create Account</a>
        </div>
    {% endif %}

</div>
{% endblock %}
